# 開發階段編排（OpenSpec Change 單位）

每個 change 對應一次 `/opsx:propose`，範圍以「可獨立驗收」為原則切分。同一階段內無依賴的 change 可並行。

## Phase 0 — 地基

### C0 `setup-project-skeleton` ✅（2026-07-12 完成）

- **目標**：專案骨架上線。
- **範圍**：Next.js (App Router + TS + Tailwind)、Drizzle + Postgres、Auth（含 demo 帳號機制的基礎）、Vercel 部署管線。
- **驗收**：公開網址可註冊登入，看到空白首頁。
- **依賴**：無。

## Phase 1 — 技能樹與核心學習循環

### C1 `skill-tree-data` ✅（2026-07-12 完成）

- **目標**：課綱成為資料，schema 通用不綁主題。
- **範圍**：技能樹 schema（Unit / Lesson、節點型態、依賴關係、掌握度欄位，主題無關）、人工策展第一條路徑 React Junior → Mid（2 Unit、約 12 節點）、每節點的考點與過關 rubric（人工撰寫，檢核引擎以此為據）、seed script。
- **驗收**：DB 有完整技能樹，API 能回傳含依賴關係的樹狀結構。
- **依賴**：C0。
- **備註**：策展課綱本身就是作者的 React 複習，不要外包給 LLM 生成。

### C2 `skill-tree-ui` ✅（2026-07-12 完成）

- **目標**：demo 核心畫面。
- **範圍**：技能樹地圖頁——節點三態（亮燈 / 裂開 / 上鎖）視覺化、GSAP 動畫、點擊節點進入課程的入口、手動標記已會節點（起點定位）。
- **驗收**：登入後看到自己的技能樹，節點狀態正確反映掌握度資料。
- **依賴**：C0、C1（可與 C3 並行）。

### C3 `lesson-session-core` ✅（2026-07-14 完成）

- **目標**：概念型節點的完整學習循環，產品的心臟。
- **範圍**：LangGraph.js session 狀態機（教學 → 蘇格拉底檢核 → 答錯換角度再教 → 掌握度更新，檢核以節點 rubric 為據）、streaming 對話 UI、語意級弱點記錄寫入、下一步推薦、使用者級 LLM 呼叫限額。
- **驗收**：完整上完一個概念型節點（如 closure），檢核通過後技能樹該節點亮燈，弱點記錄有存檔。
- **依賴**：C0、C1（可與 C2 並行）。

### C4 `practice-nodes-sandpack` ✅（2026-07-18 完成）

- **目標**：實作型節點。
- **範圍**：Sandpack 編輯器整合（課程頁右側）、實作題出題（題目 + 起始碼 + 測試）、驗收雙軌（瀏覽器內測試通過 + AI code review）、結果回寫掌握度。
- **驗收**：完整上完一個實作型節點（如寫 debounce），測試跑在瀏覽器內，AI review 意見即時顯示。
- **依賴**：C3。

> **里程碑（Phase 1 完成）**：從技能樹點進任一節點，完成「教學 → 檢核 → 亮燈」的完整循環，公開網址可體驗。

### C4.5 `lesson-experience-revamp` ✅（2026-07-18 完成）

- **目標**：概念型節點上課體感打磨（2026-07-18 自測回饋）。
- **範圍**：小單元拆分——每單元 3–5 題、做完即回饋成就感（取代一次 10 題）；答題形式多樣化——選擇、填空、配對（參考多鄰國），不再全靠口述；先教學再答題的節奏——降低單則訊息量與題目文字量，使用者理解後才進入答題；對話 prompt 續調（去僵硬感，ef3a74e 已調一輪）。
- **驗收**：上完一個概念節點，流程為多個小單元、混合題型、單則訊息輕量，單元完成有成就回饋。
- **依賴**：C3。**須排在 C5 之前**——C5 複習 session 生成補強練習會直接重用題型系統與小單元粒度，反序會重工。

### C4.6 `curriculum-regrain` ✅（2026-07-19 完成）

- **目標**：課綱粒度重構與教學深度（2026-07-18 C4.5 驗收回饋：單一節點概念太廣、開場生硬）。
- **範圍**：examPoint 升格為獨立節點（12 → 約 30，一節點一概念，參考 roadmap.sh 粒度）；課綱新增人工策展 intro 課前導入（為什麼學／應用場景／學完能做什麼，零 LLM）；節點內單元依難度遞進（基礎→進階→深入），教學訊息 150 字放寬為 200–400 字結構模板；地圖按 topic 聚群。slug 全換、舊進度作廢。
- **驗收**：地圖見 ~30 節點聚群路線；進單一節點——導入 → 分層單元 → 收尾亮燈，5–10 分鐘完課。
- **依賴**：C4.5。**建議排在 C5 之前**——C5 複習粒度綁節點，先定稿節點粒度免重工。

## Phase 2 — 個人化：記憶

### C5 `decay-review-queue`

- **目標**：「AI 記得你」的實體。
- **範圍**：掌握度時間衰減模型（衰減公式與裂開門檻在此 change 內定案）、節點裂開狀態、複習頁今日佇列（裂開節點 + 歷史弱點）、AI 依語意級弱點記錄動態生成補強練習，複習 session 刻意打向弱點。
- **驗收**：掌握度過期的節點在樹上裂開並進入佇列，複習 session 的出題可追溯到弱點記錄。
- **依賴**：C3、C4.5（複習出題重用題型系統）。

## Phase 3 — 對外呈現

### C6 `demo-account-polish`

- **目標**：讓面試官 30 秒看懂。
- **範圍**：demo 帳號預灌資料（半棵亮燈的樹、裂開節點、弱點紀錄、複習佇列）、demo 帳號 LLM 呼叫限次或預錄回放（公開網址成本防護）、landing 頁一句話說明產品、整體視覺打磨。
- **驗收**：無需註冊，點 demo 連結直接進入預灌帳號，技能樹一眼呈現產品概念。
- **依賴**：C2、C5。

## 建議執行順序

```
C0 → C1 → { C2 ∥ C3 } → C4   （里程碑：核心循環上線）
   → C4.5                    （上課體感打磨）
   → C5
   → C6                      （作品集就緒）
```
